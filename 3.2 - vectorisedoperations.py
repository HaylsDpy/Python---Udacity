# Example as to how vectors can be added together using numpy
# Should output 'array([5, 7, 9])'

import numpy as np

v1 = np.array([1, 2, 3])

v2 = np.array([4, 5, 6])

v1 + v2
v2 = np.array([4, 5, 6])
